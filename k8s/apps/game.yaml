apiVersion: v1
kind: Service
metadata:
  name: game
  namespace: snooker
  labels:
    app.kubernetes.io/name: game
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: snooker
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: game
  ports:
    - name: http
      port: 8080
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: game
  namespace: snooker
  labels:
    app.kubernetes.io/name: game
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: snooker
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: game
  template:
    metadata:
      labels:
        app.kubernetes.io/name: game
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: snooker
        app.kubernetes.io/version: v1.0.0
    spec:
      serviceAccountName: snooker-app
      containers:
        - name: game
          image: maiconda/snooker-game:v1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: PORT
              value: "8080"
            - name: ALLOWED_ORIGINS
              valueFrom:
                configMapKeyRef:
                  name: snooker-config
                  key: GAME_ALLOWED_ORIGINS
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: snooker-secrets
                  key: JWT_SECRET
            - name: INTERNAL_API_KEY
              valueFrom:
                secretKeyRef:
                  name: snooker-secrets
                  key: INTERNAL_API_KEY
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: snooker-config
                  key: GAME_DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: snooker-config
                  key: GAME_DB_PORT
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: snooker-config
                  key: GAME_DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: snooker-secrets
                  key: GAME_DB_PASSWORD
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: snooker-config
                  key: GAME_DB_NAME
            - name: DB_SSLMODE
              valueFrom:
                configMapKeyRef:
                  name: snooker-config
                  key: DB_SSLMODE
            - name: NATS_URL
              valueFrom:
                configMapKeyRef:
                  name: snooker-config
                  key: NATS_URL
            - name: GAME_OWNER_DISCONNECT_TIMEOUT_SECONDS
              valueFrom:
                configMapKeyRef:
                  name: snooker-config
                  key: GAME_OWNER_DISCONNECT_TIMEOUT_SECONDS
            - name: PROFILE_INTERNAL_URL
              valueFrom:
                configMapKeyRef:
                  name: snooker-config
                  key: PROFILE_INTERNAL_URL
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
