services:
  auth-postgres:
    image: postgres:15-alpine
    container_name: snooker-auth-postgres
    restart: always
    environment:
      POSTGRES_DB: ${AUTH_DB_NAME:-snooker_auth}
      POSTGRES_USER: ${AUTH_DB_USER:-postgres}
      POSTGRES_PASSWORD: ${AUTH_DB_PASSWORD:-postgres}
    ports:
      - "${AUTH_DB_PORT:-5432}:5432"
    volumes:
      - auth_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${AUTH_DB_USER:-postgres} -d ${AUTH_DB_NAME:-snooker_auth}"]
      interval: 5s
      timeout: 5s
      retries: 5

  auth:
    image: golang:1.25-alpine
    container_name: snooker-auth
    restart: always
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "${AUTH_PORT:-8081}:8080"
    environment:
      - PORT=8080
      - ALLOWED_ORIGINS=${AUTH_ALLOWED_ORIGINS:-http://localhost:3000}
      - COOKIE_SECURE=${AUTH_COOKIE_SECURE:-false}
      - JWT_SECRET=${JWT_SECRET:?JWT_SECRET is required}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:?GOOGLE_CLIENT_ID is required}
      - DB_HOST=auth-postgres
      - DB_PORT=5432
      - DB_USER=${AUTH_DB_USER:-postgres}
      - DB_PASSWORD=${AUTH_DB_PASSWORD:-postgres}
      - DB_NAME=${AUTH_DB_NAME:-snooker_auth}
      - DB_SSLMODE=disable
    depends_on:
      auth-postgres:
        condition: service_healthy
    command: go run auth/cmd/auth/main.go
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/health/ready || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    image: node:24-alpine
    container_name: snooker-frontend
    restart: always
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    working_dir: /app
    environment:
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:?GOOGLE_CLIENT_ID is required}
      - AUTH_API_ORIGIN=http://auth:8080
    command: sh -c "npm ci && npm run dev"
    depends_on:
      auth:
        condition: service_healthy

volumes:
  auth_postgres_data:

